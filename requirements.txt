Desktop\alpha_bot\src\config.py
╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌  1  """
  2  Configuration settings for Alpha Signal Bot
  3 +Nasdaq Options + Binance Spot signal engine
  4  """
  5
  6  import os
 ...
   9  load_dotenv()
  10
  11
  11 -class Config:
  12 -    # === Telegram Settings ===
  13 -    BOT_TOKEN: str = os.getenv("BOT_TOKEN", "")
  14 -    CHANNEL_ID: str = os.getenv("CHANNEL_ID", "")  # e.g., "@mychannel" or "-100xxxxx"
  12 +def _bool(name: str, default: str) -> bool:
  13 +    return os.getenv(name, default).strip().lower() in ("1", "true", "yes", "on")
  14
  16 -    # === Scanner Settings ===
  17 -    SCAN_INTERVAL: int = int(os.getenv("SCAN_INTERVAL", "300"))  # seconds (5 min)
  15
  19 -    # === Alpha Filters - Binance ===
  20 -    MIN_VOLUME_SPIKE_PERCENT: float = float(os.getenv("MIN_VOLUME_SPIKE", "300"))  # 300% spike
  21 -    MIN_VOLUME_USDT: float = float(os.getenv("MIN_VOLUME_USDT", "500000"))  # $500K min volume
  22 -    MAX_MARKET_CAP: float = float(os.getenv("MAX_MARKET_CAP", "50000000"))  # $50M max
  23 -    MIN_PRICE_CHANGE_PERCENT: float = float(os.getenv("MIN_PRICE_CHANGE", "3.0"))  # 3% min move
  24 -    MAX_PRICE_CHANGE_PERCENT: float = float(os.getenv("MAX_PRICE_CHANGE", "40.0"))  # filter pumped
  25 -    MIN_TRADE_COUNT: int = int(os.getenv("MIN_TRADE_COUNT", "500"))  # min tx count
  16 +def _list(name: str, default: list) -> list:
  17 +    raw = os.getenv(name, "")
  18 +    if not raw.strip():
  19 +        return default
  20 +    return [s.strip().upper() for s in raw.split(",") if s.strip()]
  21
  27 -    # === RSI Settings ===
  28 -    RSI_MIN: float = float(os.getenv("RSI_MIN", "30"))
  29 -    RSI_MAX: float = float(os.getenv("RSI_MAX", "65"))
  30 -    RSI_OVERSOLD_THRESHOLD: float = float(os.getenv("RSI_OVERSOLD", "40"))
  22
  32 -    # === DEX Settings (DexScreener) ===
  33 -    ENABLE_DEX: bool = os.getenv("ENABLE_DEX", "true").lower() == "true"
  34 -    MIN_DEX_LIQUIDITY: float = float(os.getenv("MIN_DEX_LIQUIDITY", "50000"))  # $50K
  35 -    MIN_DEX_VOLUME_24H: float = float(os.getenv("MIN_DEX_VOLUME_24H", "100000"))  # $100K
  36 -    MIN_PAIR_AGE_HOURS: int = int(os.getenv("MIN_PAIR_AGE_HOURS", "24"))  # 24 hours old min
  23 +# Curated, liquid Nasdaq-listed universe (large/mega cap, deep options chains).
  24 +# Override entirely via NASDAQ_SYMBOLS env var (comma separated) if you want a
  25 +# different watchlist.
  26 +DEFAULT_NASDAQ_SYMBOLS = [
  27 +    "AAPL", "MSFT", "NVDA", "AMZN", "GOOGL", "META", "TSLA", "AVGO", "COST",
  28 +    "ADBE", "CSCO", "AMD", "NFLX", "INTC", "QCOM", "TXN", "AMGN", "HON",
  29 +    "INTU", "AMAT", "BKNG", "SBUX", "GILD", "ADI", "ISRG", "VRTX", "REGN",
  30 +    "LRCX", "PANW", "SNPS", "CDNS", "MU", "KLAC", "MAR", "ORLY", "PYPL",
  31 +    "ABNB", "CRWD", "FTNT", "MRVL", "PEP", "MDLZ",
  32 +]
  33
  38 -    # === Contract Safety (GoPlus API) ===
  39 -    ENABLE_CONTRACT_CHECK: bool = os.getenv("ENABLE_CONTRACT_CHECK", "true").lower() == "true"
  40 -    MIN_CONTRACT_SCORE: int = int(os.getenv("MIN_CONTRACT_SCORE", "70"))
  41 -    MAX_BUY_TAX: float = float(os.getenv("MAX_BUY_TAX", "10.0"))  # max 10% buy tax
  42 -    MAX_SELL_TAX: float = float(os.getenv("MAX_SELL_TAX", "10.0"))  # max 10% sell tax
  34
  44 -    # === Buy Pressure Filter ===
  45 -    MIN_BUY_PRESSURE: float = float(os.getenv("MIN_BUY_PRESSURE", "0.55"))  # 55% buys
  35 +class Config:
  36 +    # === Telegram ===
  37 +    BOT_TOKEN: str = os.getenv("BOT_TOKEN", "")
  38 +    CHANNEL_ID: str = os.getenv("CHANNEL_ID", "")  # e.g. "@mychannel" or "-100xxxxx"
  39
  47 -    # === Signal Quality ===
  48 -    MIN_SIGNAL_SCORE: int = int(os.getenv("MIN_SIGNAL_SCORE", "65"))  # out of 100
  40 +    # === Gotham integration (generic outbound webhook/API) ===
  41 +    GOTHAM_ENABLED: bool = _bool("GOTHAM_ENABLED", "false")
  42 +    GOTHAM_WEBHOOK_URL: str = os.getenv("GOTHAM_WEBHOOK_URL", "")
  43 +    GOTHAM_API_KEY: str = os.getenv("GOTHAM_API_KEY", "")
  44 +    GOTHAM_AUTH_HEADER: str = os.getenv("GOTHAM_AUTH_HEADER", "Authorization")
  45 +    GOTHAM_AUTH_SCHEME: str = os.getenv("GOTHAM_AUTH_SCHEME", "Bearer")  # set "" for raw key
  46 +    GOTHAM_TIMEOUT_SECONDS: int = int(os.getenv("GOTHAM_TIMEOUT_SECONDS", "10"))
  47 +
  48 +    # === Scan cadence ===
  49 +    STOCK_SCAN_INTERVAL: int = int(os.getenv("STOCK_SCAN_INTERVAL", "900"))   # 15 min
  50 +    CRYPTO_SCAN_INTERVAL: int = int(os.getenv("CRYPTO_SCAN_INTERVAL", "300"))  # 5 min
  51 +    STOCK_MARKET_HOURS_ONLY: bool = _bool("STOCK_MARKET_HOURS_ONLY", "true")
  52 +
  53 +    # === Shared signal quality ===
  54 +    MIN_SIGNAL_SCORE: int = int(os.getenv("MIN_SIGNAL_SCORE", "65"))
  55      HIGH_SIGNAL_THRESHOLD: int = int(os.getenv("HIGH_SIGNAL_THRESHOLD", "80"))
  56 +    MIN_TARGET_PERCENT: float = float(os.getenv("MIN_TARGET_PERCENT", "3.0"))  # +3% minimum
  55      HIGH_SIGNAL_THRESHOLD: int = int(os.getenv("HIGH_SIGNAL_THRESHOLD", "80"))
  56 +    MIN_TARGET_PERCENT: float = float(os.getenv("MIN_TARGET_PERCENT", "3.0"))  # +3% minimum
  57 +    ATR_TARGET_MULT: float = float(os.getenv("ATR_TARGET_MULT", "1.5"))
  58 +    ATR_STOP_MULT: float = float(os.getenv("ATR_STOP_MULT", "1.2"))
  59
  51 -    # === API Keys (Optional) ===
  52 -    CMC_API_KEY: str = os.getenv("CMC_API_KEY", "")  # CoinMarketCap
  53 -    GOPLUS_API_KEY: str = os.getenv("GOPLUS_API_KEY", "")  # GoPlus Safety
  60 +    # === Nasdaq / Options (stocks) ===
  61 +    NASDAQ_SYMBOLS: list = _list("NASDAQ_SYMBOLS", DEFAULT_NASDAQ_SYMBOLS)
  62 +    OPTIONS_PROVIDER: str = os.getenv("OPTIONS_PROVIDER", "polygon")  # polygon | none
  63 +    POLYGON_API_KEY: str = os.getenv("POLYGON_API_KEY", "")
  64 +    OPTIONS_MAX_EXPIRY_DAYS: int = int(os.getenv("OPTIONS_MAX_EXPIRY_DAYS", "45"))
  65 +    MIN_OPTIONS_CONTRACTS_VOLUME: int = int(os.getenv("MIN_OPTIONS_CONTRACTS_VOLUME", "500"))
  66 +    PUT_CALL_BULLISH_MAX: float = float(os.getenv("PUT_CALL_BULLISH_MAX", "0.7"))   # <= => bullish
  67 +    PUT_CALL_BEARISH_MIN: float = float(os.getenv("PUT_CALL_BEARISH_MIN", "1.3"))   # >= => bearish, filtered out
  68 +    STOCK_RSI_MIN: float = float(os.getenv("STOCK_RSI_MIN", "35"))
  69 +    STOCK_RSI_MAX: float = float(os.getenv("STOCK_RSI_MAX", "70"))
  70 +    MIN_STOCK_VOLUME_SPIKE: float = float(os.getenv("MIN_STOCK_VOLUME_SPIKE", "50"))  # % vs avg
  71
  55 -    # === Pairs to scan (quote currencies) ===
  56 -    QUOTE_CURRENCIES: list = ["USDT", "BUSD"]
  72 +    # === Binance Spot (crypto) ===
  73 +    QUOTE_CURRENCIES: list = ["USDT"]
  74 +    MIN_VOLUME_USDT: float = float(os.getenv("MIN_VOLUME_USDT", "5000000"))  # $5M+ = liquid majors
  75 +    MIN_PRICE_CHANGE_PERCENT: float = float(os.getenv("MIN_PRICE_CHANGE", "1.0"))
  76 +    MAX_PRICE_CHANGE_PERCENT: float = float(os.getenv("MAX_PRICE_CHANGE", "40.0"))
  77 +    MIN_TRADE_COUNT: int = int(os.getenv("MIN_TRADE_COUNT", "1000"))
  78 +    MIN_VOLUME_SPIKE_PERCENT: float = float(os.getenv("MIN_VOLUME_SPIKE", "150"))
  79 +    CRYPTO_RSI_MIN: float = float(os.getenv("CRYPTO_RSI_MIN", "30"))
  80 +    CRYPTO_RSI_MAX: float = float(os.getenv("CRYPTO_RSI_MAX", "68"))
  81 +    MIN_BUY_PRESSURE: float = float(os.getenv("MIN_BUY_PRESSURE", "0.55"))
  82
  58 -    # === Blacklisted coins (stables, wrapped, etc.) ===
  83      BLACKLIST: set = {
  84          "USDT", "BUSD", "USDC", "DAI", "TUSD", "USDP", "FRAX",
  85          "WBTC", "WETH", "WBNB", "BTCB",
  62 -        "BTC", "ETH", "BNB", "XRP", "ADA", "SOL", "DOGE",
  63 -        "SHIB", "MATIC", "DOT", "AVAX", "LINK", "UNI", "AAVE",
  64 -        "LUNA", "UST", "LUNC"
  86      }
  87 +
  88 +    # === Persistence ===
  89 +    DEDUPE_HOURS: int = int(os.getenv("DEDUPE_HOURS", "6"))
╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌ Do you want to overwrite config.py?
   1. Yes
 > 2. Yes, allow all edits during this session (shift+tab)
   3. No

 Esc to cancel

  11 tasks (0 done, 1 in progress, 10 open)
  ■ Rewrite config.py
  □ Build technical.py indicators
  □ Build options_analyzer.py
  □ Build stock_scanner.py
  □ Rewrite crypto_scanner.py (Binance Spot)
   … +6 pending
